# Зависимости
node_modules/
dist/
dist-ssr/

# Tauri / Rust
src-tauri/target/
src-tauri/gen/

# Бинарники sidecar (скачиваются скриптом fetch-binaries — НЕ коммитим)
src-tauri/binaries/

# Flatpak (артефакты сборки; манифест и desktop/metainfo — коммитятся)
flatpak/mestia.deb
flatpak/repo/
flatpak/flatpak-build/
.flatpak-builder/
*.flatpak

# Скачанные/собранные артефакты релиза (в корне) — НЕ коммитим
/latest.json
Mestia_*.app.tar.gz
Mestia_*.app.tar.gz.sig
*.AppImage
*.dmg
*.deb
*.msi
*.rpm
*-setup.exe

# Ключи подписи апдейтера (приватный ключ — НИКОГДА не коммитим)
*.key
*.key.pub
mestia.key

# Логи и системное
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# (иконки src-tauri/icons и src-tauri/installer КОММИТЯТСЯ — нужны для сборки)
